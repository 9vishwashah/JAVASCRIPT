let exp = `satsv`
switch (exp) {
    case `nwh`:
        console.log(`no way home`);
        break
    case `satsv`:
    case `tdk`:
        console.log(`best`);
        break
    default:
        console.log(`all are greatest`);
}